/**
 * @class State
 * @description Manages global state for the Expense Tracker.
 */
class State {
	// Month/Year date pickers have been removed. The dashboard is now "Now" focused.
}

export const appState = new State();
